(ns examples.i64-semantics)

(defn add [x y] (+ x y))
(defn subtract [x y] (- x y))
(defn multiply [x y] (* x y))
(defn negate [x] (- x))
(defn choose [condition then-value else-value]
  (if condition then-value else-value))
(defn divide [x y] (quot x y))

(defn main [] 0)
